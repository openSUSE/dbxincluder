<services>
  <service name="tar_scm" mode="disabled">
    <param name="url">https://github.com/openSUSE/dbxincluder.git</param>
    <param name="scm">git</param>
    <param name="filename">python3-dbxincluder</param>
    <param name="version">0.9~develop</param>
    <param name="revision">refs/heads/develop</param>
  </service>
  <service name="recompress" mode="disabled">
    <param name="file">*.tar</param>
    <param name="compression">xz</param>
  </service>
  <service name="set_version" />
</services>
